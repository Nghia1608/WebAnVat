<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">Trang chủ</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      {{!-- <li class="nav-item active">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li> --}}
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Danh mục sản phẩm
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="#">Đồ khô</a>
          <a class="dropdown-item" href="#">Đồ khô 2</a>
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href="#">Coming soon</a>
        </div>
      </li>
      <li class="nav-item">
        <a class="nav-link disabled" href="/users/cart">Giỏ hàng</a>
      </li>
      <li class="nav-item">
        <a class="nav-link disabled" href="/auth/login">Đăng nhập</a>
      </li>
        <li class="nav-item">

        <a class="nav-link disabled"href="/auth/logout">Đăng xuất</a>
      </li>
      <li class="nav-item dropdown" id="afterLogin">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            QQ j day
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="/users/{{this._id}}/edit">Đổi mật khẩu</a>
          <a class="dropdown-item" href="#">Đồ khô 2</a>
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href="#">Coming soon</a>
        </div>
      </li
    </ul>
    
  </div>

</nav>
  <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
    </form>

    <script>
      
      window.onload = function(req,res,next){
            document.getElementById("afterLogin").hidden = true;
            verifyToken(req, res, () => {
              if (req.user.id || req.params.id) {
                console.log('test user',req.user.username)

                document.getElementById("afterLogin").hidden = false;
                document.getElementById("navbarDropdown").innerHTML = req.body.username;
                next();
              } else {
                res.status(403).json("You're not allowed to do that!");
              }
            });              
          }

          const verifyToken = (req, res, next) => {
          //ACCESS TOKEN FROM HEADER, REFRESH TOKEN FROM COOKIE
          const refreshToken = req.cookies.refreshToken;
          if (refreshToken) {
            const accessToken = refreshToken;
            jwt.verify(accessToken, process.env.JWT_ACCESS_KEY, (err, user) => {
              if (err) {
                res.status(403).json("Token is not valid!");
              }
              req.user = user;
              next();
            });
          } else {
            res.redirect('auth/login')
          }
        };

    </script>

